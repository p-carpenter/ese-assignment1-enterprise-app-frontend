import { PlaylistList } from "../components";

/**
 * Full page showing playlists. Currently composes `PlaylistList`.
 * @returns Playlists page element.
 */
export const PlaylistsPage = () => {
  return (
    <>
      <PlaylistList />
    </>
  );
};
